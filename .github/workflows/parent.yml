on:
  workflow_dispatch:

jobs:
  caller:
    strategy:
      matrix:
        # 2 batches * 256 jobs each = 512 total jobs
        consumers: ['batch0', 'batch1']
    uses: ./.github/workflows/child.yml
    with:
      batch: ${{ matrix.consumers }}
